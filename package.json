{
    "name": "handmedown",
    "version": "0.0.1",
    "scripts": {
        "start": "node ./server/bin/www",
        "heroku-postbuild": "cd server && npm install && cd ../client && npm install --only=prod && npm run build"
    },
    "dependencies": {
        "bcrypt": "^4.0.0",
        "body-parser": "^1.19.0",
        "cookie-parser": "^1.4.4",
        "cors": "^2.8.5",
        "dotenv": "^8.2.0",
        "express-session": "^1.17.0",
        "multer": "^1.4.2",
        "passport": "^0.4.1",
        "passport-local": "^1.0.0",
        "toidentifier": "^1.0.0"
    }
}
